/**
 * Chart definition
 * 
 * NOTE: It is fitting with UI now so all sub-objects are string
 */
export interface Chart {
    uri: string;
    name: string;
    flows: string;
    prompts: string;
    datasets: string;
    functions: string;
}

